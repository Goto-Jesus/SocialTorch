let initialState = {
    friends: [
        { id: 1, name: "Katya" },
        { id: 2, name: "Vadya" },
        { id: 3, name: "Max" }
    ]
};

const sidebarReducer = (state = initialState,action) =>{

    return state;
}

export default sidebarReducer;